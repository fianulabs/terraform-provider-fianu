self-hosted-runner:
  # Custom labels actionlint should recognize. Currently empty —
  # workflows use stock GitHub-hosted runners (ubuntu-latest,
  # ubuntu-latest-16-cores). Add labels here if a future workflow
  # adopts the org's self-hosted/managed runner pool.
  labels: []
